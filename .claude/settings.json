{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; f=\"${f%$'\r'}\"; case \"$f\" in *.ts|*.tsx) node \"${CLAUDE_PROJECT_DIR:-.}/packages/frontend-web/scripts/check-design-invariants.mjs\" \"$f\" ;; esac; } 2>/dev/null || true",
            "timeout": 15,
            "statusMessage": "Pruefe Design-Invarianten"
          }
        ]
      }
    ]
  }
}
