# DOM-CON-004 — UAT-Nachweispaket Kontrakt-Lebenszyklus (2026-06-11)

Abnahme der Kontrakt-Tiefe (Slices 004.1–004.4) end-to-end gegen die DEMO-Seed-
Kontrakte. Schließt DOM-CON-004 ab (Slice 004.5).

## Browser-Smoke (Playwright, @smoke)
`playwright-tests/specs/agrar/kontrakt-lifecycle-smoke.spec.ts` — lädt die drei
Arbeitsräume und prüft die Kopfzeile:
- `/agrar/kontrakt-fixierung` → „Kontrakt-Fixierung"
- `/agrar/kontrakt-engagement` → „Kontrakt-Engagement"
- `/agrar/kontrakt-settlement` → „Kontrakt-Settlement"

Lauf (CI / Preview-Build): `npx playwright test specs/agrar/kontrakt-lifecycle-smoke.spec.ts --project=smoke`

**Hinweis zur lokalen Umgebung:** Die `@smoke`-Suite nutzt die gemeinsame Login-
Fixture (`fixtures/testSetup.ts`) und erwartet den Preview-Build (Default-baseURL
:4173) mit CI-Auth/Seed. Gegen den laufenden **Vite-Dev-Server :3000** schlägt die
Fixture-Authentifizierung fehl — und zwar identisch für **alle** bestehenden Specs
(gegengeprüft mit `duenger-smoke.spec.ts`: ebenfalls 3/3 „browser closed"). Die neue
Spec verhält sich also exakt wie die übrige Suite; die Browser-Abnahme läuft in CI.
Der fachliche Nachweis erfolgt hier über das Live-UAT (unten, grün).

## Live-UAT (fachliche Kette)
`scripts/uat/con_contract_lifecycle_uat.py` — treibt die öffentliche API gegen Seed,
mit DB-Cleanup am Ende (erzeugte Zeilen per ID gelöscht, mutierte Seed-Zeilen
zurückgesetzt). Lauf:

```
docker exec -e PYTHONPATH=/app -e VALEO_API_BASE_URL=http://localhost:8000 \
  valeo-neuro-erp-backend python scripts/uat/con_contract_lifecycle_uat.py --execute
```

### Geprüfte Kette (alle ✓, Status `passed`)
1. Fixierungs-Arbeitsraum DEMO-KT-004: Baseline 300/500 fixiert, Mark-to-Market
   bewertbar (Bewertung fixiert **+99,99 €**, offener Marktwert **44.000 €**).
2. Engagement: offenes MATIF-Verkaufsengagement „Weizen B MATIF" 500; Netto **−480**.
3. Kontraktmahnung: DEMO-KT-002 ist Mahnkandidat (überfällig, offen > 0).
4. Guard: Überfixierung (9999 t) → **422**.
5. Teilfixierung 50 t @ 207 → offen 150; Workspace zeigt 350 fixiert.
6. Kontraktmahnung erfasst → Mahnstufe 1.
7. Settlement-Übergabe DEMO-KT-001 → **120 t abgerechnet**.
8. Storno gebuchter Bewegung → **422** (fail-closed).
9. Fixierungs-Storno → gibt **50 t** frei.

### Evidenz (JSON)
```json
{"status": "passed", "bewertung_fixiert_eur": 99.99, "marktwert_offen_eur": 44000.0, "engagement_netto": -480.0}
```

## Backend-Unit-Tests (kumuliert, 18 grün)
`tests/test_contract_fulfillment.py` (6) · `test_contract_fixing.py` (6) ·
`test_contract_engagement.py` (3) · `test_contract_settlement.py` (3).

## Damit abgeschlossen
DOM-CON-004.1 Erfüllungsstand · 004.2 Fixierung/MATIF · 004.3 Engagement/Mahnung ·
004.4 Settlement/Storno · 004.5 E2E+UAT.
