# DOM-PROC-004 — UAT-Nachweispaket Beschaffungs-Match (2026-06-11)

Abnahme der Beschaffungs-Tiefe (Slices 004.1–004.5) gegen DEMO-Seed-Bestellungen.
Schließt DOM-PROC-004 ab.

## Browser-Smoke (Playwright, @smoke)
`playwright-tests/specs/einkauf/procurement-match-smoke.spec.ts` — lädt den
Wareneingangs-Abgleich und prüft die Kopfzeile.

Lauf (CI / Preview-Build):
`npx playwright test specs/einkauf/procurement-match-smoke.spec.ts --project=smoke`

## Live-UAT (fachliche Kette)
`scripts/uat/proc_match_lifecycle_uat.py` — treibt Match-Spine, Rechnungsstufe,
Folgeaktion und ERS-Gutschrift gegen Seed; räumt erzeugte Zeilen am Ende auf.

```
docker exec -e PYTHONPATH=/app -e VALEO_API_BASE_URL=http://localhost:8000 \
  valeo-neuro-erp-backend python scripts/uat/proc_match_lifecycle_uat.py --execute
```

### Geprüfte Kette (alle ✓, Status `passed`)
1. `DEMO-PO-001`: 3-Wege abgeglichen (27.700 € geliefert = fakturiert).
2. `DEMO-PO-002`: Überlieferung 22/20 t → ERS-Vorschau **960 €**.
3. Folgeaktion Reklamation erfasst (append-only).
4. ERS-Gutschrift erzeugt und bereinigt.

### Evidenz (JSON)
```json
{"status": "passed", "po001_geliefert_wert": 27700.0, "po002_ers_preview": 960.0}
```

## Backend-Unit-Tests (kumuliert, 18 grün)
`tests/test_procurement_match.py` (6) · `test_procurement_three_way_match.py` (4) ·
`test_procurement_follow_up.py` (4) · `test_procurement_ers.py` (4).

## Damit abgeschlossen
DOM-PROC-004.1 Match-Spine · 004.2 Rechnungsstufe · 004.3 Seed+UI ·
004.4 Folgeaktionen · 004.5 ERS+UAT.
