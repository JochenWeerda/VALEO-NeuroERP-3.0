slice_id: DOM-POS-004
title: "POS-Domäne — Tagesabschluss Lifecycle (.2–.5): Z-Bon, TSE-Simulation, DSFinV-K"
owner: Claude Sonnet 4.6
status: completed
goal: >-
  POS-Kassentagesabschluss auf volle 004-Tiefe heben: OFFEN→IN_ABSCHLUSS→Z_BON_ERSTELLT
  →TSE_SIGNIERT→DSFINVK_EXPORTIERT→ABGESCHLOSSEN. Simulierte TSE für Tests.
  Externe TSE-/DSFinV-K-Abnahme bleibt Gate.
file_ownership:
  - docs/agent-ops/slices/DOM-POS-004.yaml
  - app/services/pos_tagesabschluss_service.py
  - alembic/versions/pos_tagesabschluss_lifecycle_20260623.py
  - app/api/v1/endpoints/pos_tagesabschluss_actions.py
  - tests/test_dom_pos_004.py
not_owned:
  - app/api/v1/endpoints/pos_dsfinvk.py
  - app/api/v1/endpoints/pos_fiscalization.py
acceptance_criteria:
  - Lifecycle OFFEN→ABGESCHLOSSEN + FEHLER-Pfad
  - TSE-Simulation deterministisch + Hinweis "kein echtes TSE"
  - 7 Unit-Tests grün
ai_harness:
  fachlicher_vertrag: >-
    Z-Bon-Nr eindeutig je Kasse+Datum. TSE-Signatur append-only (kein Update).
    DSFinV-K-Export: extern gegated, Pfad wird nur als TEXT gespeichert.
  architektur_vertrag: Schema domain_pos via Alembic. text()-SQL only.
  daten_vertrag: pos_tagesabschluesse (UNIQUE tenant+kasse+datum), pos_tagesabschluss_log.
  test_vertrag: Lifecycle-Übergänge + TSE-Simulation + FEHLER-Pfad.
  security_vertrag: X-Tenant-ID + UNIQUE Constraint verhindert Doppelabschluss.
  betriebs_vertrag: create_tagesabschluss idempotent via kasse_id+datum.
  dokumentations_vertrag: Workboard-Eintrag + Slice-YAML
external_gates:
  - Echte TSE-Abnahme (reales TSE-Gerät)
  - DSFinV-K-2.4 Prüfwerkzeug-Abnahme
