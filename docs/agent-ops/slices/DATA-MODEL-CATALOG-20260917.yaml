---
slice_id: DATA-MODEL-CATALOG
id: DATA-MODEL-CATALOG
title: Tabellenkatalog, Besitz und Masken-Lebenszyklus
status: offen
owner: unassigned
date: 2026-09-17
created_at: 2026-09-17
coordination: >
  P2 geliefert. Naechster Claim ist P3 (Verbraucher-Lineage Tabelle →
  Endpunkt → ScreenDefinition), nicht P4 gleichzeitig. Keine FSX-/Rechnungsdateien.
goal: >
  Jede domain_*-Tabelle hat Schema-Custodian und Architecture-Domain.
  Unbekanntes Schema und Praefix-Konflikt ohne Legacy-Zeile fallen (only-up).
file_ownership:
  - docs/agent-ops/todo-datenmodell-katalog.md
  - docs/entwickler/datenmodell-tenancy.md
  - scripts/generate_table_catalog.py
  - scripts/table_ownership.py
  - scripts/check_domain_table_ownership.py
  - tests/test_generate_table_catalog.py
  - tests/test_table_ownership.py
  - docs/admin/table-catalog.md
  - docs/admin/table-catalog.json
acceptance:
  - python scripts/check_domain_table_ownership.py Exit 0 (635 Tabellen, 13 Legacy)
  - Katalog-JSON nennt owner_domain und placement je Tabelle
  - Architecture Index database_schemas listet die physischen Schemas je Domain
tests:
  - tests/test_table_ownership.py
  - tests/test_generate_table_catalog.py
risks:
  - Praefixe crm_/finance_/hrm_ wurden bewusst nicht erweitert (zu viele Altlagen)
  - Legacy-Liste darf nur wachsen mit Begruendung, nicht als stilles Schlupfloch
ai_harness:
  fachlicher_vertrag: >
    Agenten und Entwickler sehen Schema, Tabelle und Spalten aus der laufenden
    DB, nicht aus einem handgezeichneten UML. Zwei Fachmodelle bleiben zwei
    Tabellen.
  architektur_vertrag: >
    ADR-003 bleibt logisch. Physische Wahrheit ist information_schema.
    Architecture-OS-Generator-Muster (--check, nicht manuell editieren).
  daten_vertrag: >
    Nur domain_*-BASE TABLE. Tenant-Spalten werden gelistet, nicht interpretiert.
    Kein DROP, kein Rewrite fremder Migrationen.
  test_vertrag: >
    Unit-Render gegen Fixture. Integration gegen echte Postgres (require_db).
  security_vertrag: >
    DATABASE_URL aus der Umgebung, keine Secrets im Katalog. Nur Metadata-SELECT.
  betriebs_vertrag: >
    Qualitaetsgate nach Alembic. Rollback = Generator-Commit revertieren;
    die Datenbank bleibt unangetastet.
  dokumentations_vertrag: >
    Workboard, todo-datenmodell-katalog.md P2, table_ownership.py, Architecture Index database_schemas.
external_gates:
  - not_applicable
---
