slice_id: HR-TIME-PAYROLL-CLOSE-001
title: "HR-TIME UX-M5 — Payroll Closeout Gate (Export erst moeglich wenn Blocker geloest)"
status: umgesetzt
wave: 38
date: 2026-06-27
owner: Claude Code
goal: >-
  Payroll-Export-Button ist gesperrt solange Blocker existieren. Nutzer sieht
  klare Gate-Meldung mit per-Blocker-Aufloesung und Direktlink zum Steuerungs-Tab.
  Bei Blocker=0 und Status=ready ist Export moeglich.
file_ownership:
  - packages/frontend-web/src/pages/personal/zeiterfassung.tsx
  - docs/project-context/hr-time-ux-workflow-roadmap-2026-05-12.md
  - docs/agent-ops/slices/HR-TIME-PAYROLL-CLOSE-001.yaml
acceptance:
  - Header-Button disabled wenn blockers.length > 0 oder status != 'ready'.
  - Agent-Tab-Button hat denselben Guard.
  - Payroll-Tab zeigt rotes Gate-Banner mit per-Blocker-Aufloesung und Pruefen-Link.
  - Payroll-Tab zeigt gruenes Freigabe-Banner mit Export-Button wenn kein Blocker.
  - tsc --noEmit exit 0.
  - UX-M5 in hr-time-ux-workflow-roadmap als umgesetzt markiert.
tests:
  - tsc --noEmit (exit 0 verified)
ai_harness:
  fachlicher_vertrag: >-
    Erfordert cockpit.payrollReadiness.status === 'ready' UND blockers.length === 0
    fuer Export-Freigabe. Entspricht dem Roadmap-Akzeptanzkriterium.
  architektur_vertrag: >-
    Kein neuer API-Aufruf. Guard direkt auf cockpit.payrollReadiness-Daten aus
    useTimeCockpit. Mutation isPending zusaetzlich als Duplicate-Submit-Guard.
  daten_vertrag: >-
    Keine DB-Aenderungen.
  test_vertrag: >-
    TypeScript 0 Fehler. Mutation-Lifecycle-Invariant (CLAUDE.md) eingehalten:
    disabled + isPending + finally-Reset via useMutation.
  security_vertrag: >-
    Keine neuen API-Calls. Bestehender createPayrollExport-Guard unveraendert.
  betriebs_vertrag: >-
    Export-Gate ist rein UI-seitig — Backend kann Export trotzdem erzwingen.
    Fachliche Bedeutung: HR muss Blocker aktiv auflösen oder begruenden.
  dokumentations_vertrag: >-
    hr-time-ux-workflow-roadmap UX-M5 als umgesetzt markiert.
