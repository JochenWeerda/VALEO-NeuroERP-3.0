# 2026-02-10

## CI/Quality Gate
- Recovered staged-but-missing CI/security files into working tree and normalized encodings.
- `.gitignore`: hardened against accidental commits (`.env*`, sqlite artifacts, `de_modules/`, `memory-bank/`, `.backups/`, `gitleaks-report.json`, `nul`, and a miscreated nested workspace dir pattern).
- Removed `memory-bank/` and `de_modules/` from git index (keep on disk, stop tracking).

## Alembic / DB Determinism
- Added a merge migration to enforce a single Alembic head: `alembic/versions/4b6600ac3926_merge_heads.py`.
- Fixed multiple migrations so a fresh ephemeral Postgres can migrate to `head` deterministically:
  - `alembic/versions/add_inventory_entities_stock_movements_and_inventory_counts.py` (UUID FK type consistency)
  - `alembic/versions/4601a09c0fc1_add_farmer_declaration_fields_to_psm.py` (table-existence guard + schema-correct add/drop + short revision id)
  - `alembic/versions/add_agrar_domain_models_saatgut_duenger_psm_biostimulanzien.py` (UUID id/tenant_id consistency)
  - `alembic/versions/a489a6a4a212_add_l3_target_tables.py` (ensure schemas exist before creating tables)
  - `alembic/versions/add_documents_json_table.py` (correct JSONB indexing: GIN on `data`, btree expression indexes)
  - `alembic/versions/add_portal_tables_contracts_prepurchases_orders.py` (ENUM creation idempotent; avoid duplicate type creation)
  - `alembic/versions/add_procurement_p0_tables.py` (guard `einkauf_bestellungen` alter if table absent)
- Verified locally: `python scripts/check_alembic_single_head.py` passes; `python scripts/init_db.py` migrates a blank Postgres container to head.

## Scripts
- `scripts/init_db.py`: added retry loop on transient DB connection failures to reduce CI flakes.


