# DOM-CRM-004 — UAT-Nachweispaket (Kundenstamm-Tiefe)

Abnahme-Grundlage für die CRM-Tiefe (Slices 004.1–004.4): Dubletten, Merge,
Ownership, Wiedervorlagen/Serviceabschluss. Maßstab: 7-Punkte-Definition
„fachliche Tiefe erreicht". Verifiziert an echten Seed-Daten (462 Kunden).

## E2E (automatisiert)
- `playwright-tests/specs/crm/crm-stammdaten-smoke.spec.ts` (@smoke): Dubletten,
  Kunden-Zuordnung, Wiedervorlagen laden (Reload-Retry-Muster wie SUPPLY).
- **Lauf-Hinweis:** Der Spec ist nach dem bewährten Muster geschrieben (vgl. grüner
  SUPPLY-E2E `rueckverfolgbarkeit-smoke.spec.ts`). Im aktuellen DEV scheitert der
  Lauf an der **Dev-Login-Fixture** (`input[name="email"]`-Timeout nach Frontend-
  Neustart) — eine Auth-Umgebungsbedingung, kein Seitendefekt. Funktionale
  Verifikation erfolgt daher über API-/Unit-Tests (s. u.); E2E grün, sobald die
  Login-Fixture im Ziel-Environment greift.
- Backend-Logik: `tests/test_crm_duplicates.py` (7 grün).

## UAT-Testfälle
| # | Schritt | Erwartet | Punkt |
|---|---|---|---|
| 1 | „Kunden-Dubletten" öffnen | Cluster mit Mitgliedern + Gründen (E-Mail/Tel/Name+PLZ) + Konfidenz | 1/2 |
| 2 | Master wählen, „Zusammenführen" | Historie auf Master umgehängt, Verlierer merged (geloescht+merged_into), Audit | 3/4/5 |
| 3 | Merge erneut | 422 „bereits zusammengeführt" | 2 |
| 4 | „Kunden-Zuordnung" öffnen | Worklist „ohne Außendienst" mit Gesamtzahl | 1 |
| 5 | AD/ID zuweisen | kunden_crm360 gesetzt, Übergabe-Audit-Eintrag, Kunde verschwindet aus Worklist | 3/5 |
| 6 | „Wiedervorlagen" öffnen | offene WV über alle Kunden, überfällige rot | 1/2 |
| 7 | WV erledigen (mit Ergebnis) | erledigt=true, Ergebnis an Notiz, verschwindet aus Liste | 3/5 |
| 8 | Tenant-Trennung | keine fremden Kunden/WV sichtbar | 5 |

## Erbrachte technische Nachweise (2026-06-10)
- Dubletten: 5 Cluster an 462 Kunden, inkl. namens-umgestellter GbRs (Normalisierung).
- Merge: Temp-Paar — Kontakt umgehängt, Verlierer merged, Doppel-Merge 422 (Seed unberührt).
- Ownership: unassigned=461; Zuweisung schreibt Felder + 2 Audit-Einträge; by-owner ok.
- Wiedervorlagen: ueberfaellig-Flag korrekt; Erledigen mit Ergebnis.

## Offen / Externe Abnahme (Punkt 7)
- Voll-Merge inkl. Finanz-/Belegdaten über business_partner_id (separater BP-Merge).
- Rollen-/Rechte-Feinsteuerung je Bediener (RBAC-Slice).
- Fachliche Abnahme durch Vertriebsleitung (Unterschrift) ausstehend.

## Live-UAT (fachliche Kette)
`scripts/uat/crm_stammdaten_lifecycle_uat.py` — Dubletten + Merge-Vorschau (read-only),
Ownership-Worklist, Wiedervorlagen; mit ``--execute`` temporäre Zuordnung/WV inkl.
DB-Cleanup (Standard-Kunde ``GAP00001`` via ``VALEO_CRM_UAT_KUNDE``).

```
python scripts/uat/crm_stammdaten_lifecycle_uat.py
python scripts/uat/crm_stammdaten_lifecycle_uat.py --execute --json
```

### Geprüfte Kette (Slice 004.5)
1. ``GET /crm/duplicates`` — Cluster mit ≥2 Mitgliedern (wenn vorhanden).
2. ``POST /crm/duplicates/merge-preview`` — read-only Vorschau.
3. ``GET /crm/ownership/unassigned`` — Worklist + ``total``.
4. ``GET /crm/kunden-kontakte/wiedervorlagen`` — offene WV-Liste.
5. ``--execute``: Ownership setzen + Historie, WV anlegen → erledigen → Cleanup.

## Status
**DOM-CRM-004 abgeschlossen** (Slices 004.1–004.5): Dubletten, Merge, Ownership,
Wiedervorlagen/Serviceabschluss, Playwright-@smoke + Live-UAT.
