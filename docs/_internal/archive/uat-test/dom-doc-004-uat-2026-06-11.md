# DOM-DOC-004 — UAT-Nachweispaket Nachweisraum-Lebenszyklus (2026-06-11)

Abnahme der GoBD-Nachweisraum-Tiefe (Slices 004.1–004.4) end-to-end. Schließt
DOM-DOC-004 ab (Slice 004.5).

## Browser-Smoke (Playwright, @smoke)
`playwright-tests/specs/docflow/nachweisraum-lifecycle-smoke.spec.ts` — lädt
Artefakt-Upload/Freigabe, Wiedervorlagen/Bescheide, GoBD-Export. Login-Fixture-Grenze
lokal wie CON/SALES/FIN-004.5 (CI-Preview :4173); Browser-Abnahme in CI.

## Live-UAT (fachliche Kette)
`scripts/uat/doc_nachweisraum_lifecycle_uat.py --execute` (mit DB-Cleanup). Lauf:

```
docker exec -e PYTHONPATH=/app -e VALEO_API_BASE_URL=http://localhost:8000 \
  valeo-neuro-erp-backend python scripts/uat/doc_nachweisraum_lifecycle_uat.py --execute
```

### Geprüfte Kette (alle ✓, Status `passed`)
1. Evidence-Sicht des Vorgangs gefunden.
2. Paperless-Probe liefert ehrlichen Status (in DEV „nicht konfiguriert").
3. Artefakt-Upload mit SHA-256 (Versionierung).
4. Artefakt freigegeben (entwurf→freigegeben).
5. Wiedervorlage (überfällig) erscheint in der Worklist.
6. GoBD-Exportpaket mit Prüfsumme (SHA-256) erzeugt.
7. Cleanup (Artefakt/Followup entfernt, exported_at zurückgesetzt).

### Evidenz (JSON)
```json
{"status": "passed", "paperless_konfiguriert": false, "pruefsumme": "d8936a0a608e"}
```

### Robustheits-Fund (vom UAT aufgedeckt & behoben)
Fremder `artifactType` verletzte den DB-CHECK `chk_doc_artifact_type` → **500**.
`docflow_artifact_service.upload_artifact` validiert den Typ jetzt vorab (`pdf/xml/
html/other`) und liefert **422** statt 500.

## Backend-Unit-Tests (kumuliert, 18 grün)
`test_docflow_evidence.py` (5) · `test_docflow_artifact.py` (5) ·
`test_docflow_followup.py` (5) · `test_docflow_gobd.py` (4).

## Damit abgeschlossen
DOM-DOC-004.1 Nachweis-Spine · 004.2 Artefakt-Upload/Versionierung/Freigabe ·
004.3 Bescheid/Wiedervorlage · 004.4 GoBD-Export/Paperless · 004.5 E2E+UAT.
**Extern gegated (ehrlich):** Paperless-Liveprobe (DEV ohne PAPERLESS_URL).
