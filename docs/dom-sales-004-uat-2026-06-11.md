# DOM-SALES-004 — UAT-Nachweispaket O2C-Lebenszyklus (2026-06-11)

Abnahme der O2C-Tiefe (Slices 004.1–004.4) end-to-end gegen die DEMO-Sales-Seed.
Schließt DOM-SALES-004 ab (Slice 004.5).

## Browser-Smoke (Playwright, @smoke)
`playwright-tests/specs/sales/o2c-lifecycle-smoke.spec.ts` — lädt die drei O2C-
Arbeitsräume und prüft die Kopfzeile:
- `/sales/auftrag-lieferschein-abgleich` → „Auftrag-Lieferschein-Abgleich"
- `/sales/kreditlimit-pruefung` → „Kreditlimit-Prüfung"
- `/sales/lieferung-storno` → „Lieferung-Storno / Gutschrift"

**Hinweis (wie CON-004.5):** Die `@smoke`-Login-Fixture authentifiziert lokal nur
gegen den CI-Preview-Build (:4173), nicht gegen den Vite-Dev-Server :3000 — identisch
für alle bestehenden Specs. Browser-Abnahme läuft in CI; fachlicher Nachweis hier via
Live-UAT.

## Live-UAT (fachliche Kette)
`scripts/uat/sales_o2c_lifecycle_uat.py --execute` (mit DB-Restore). Lauf:

```
docker exec -e PYTHONPATH=/app -e VALEO_API_BASE_URL=http://localhost:8000 \
  valeo-neuro-erp-backend python scripts/uat/sales_o2c_lifecycle_uat.py --execute
```

### Geprüfte Kette (alle ✓, Status `passed`)
1. Positions-Match DEMO-AUF-001: MILCHLEISTUNG-18 vollständig (25/25),
   MINERAL-RIND teilgeliefert (3/5, 2 offen), Abweichung.
2. Kreditlimit-Ampel: Auslastung mit Auftrag **86,5 %** → **warnung**.
3. Guard: Storno auf berechnete Lieferung → **422**.
4. Durchgängiger Storno (uninvoiced): Lieferschein storniert → MILCHLEISTUNG-18
   im Match wieder **offen** (0 geliefert).
5. Seed-Restore (Lieferschein aktiv + Rechnung).

### Evidenz (JSON)
```json
{"status": "passed", "match_hat_abweichung": true, "kredit_ampel": "warnung"}
```

## Backend-Unit-Tests (kumuliert, 14 grün)
`test_sales_match.py` (5) · `test_sales_credit.py` (5) · `test_sales_storno.py` (4).

## Damit abgeschlossen
DOM-SALES-004.1 O2C-Spine · 004.2 Positions-Match · 004.3 Kreditlimit/Billing ·
004.4 Storno/Gutschrift · 004.5 E2E+UAT.
