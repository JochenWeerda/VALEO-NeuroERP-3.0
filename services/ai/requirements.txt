# VALEO-NeuroERP AI-Service Requirements
# Microservice for AI/ML functionality

# Core FastAPI and async support
# fastapi muss mit dem Root-Pin gleichlaufen: starlette==1.3.1 (CVE-Stand) wird erst
# ab fastapi 0.136.x akzeptiert; 0.121.3 verlangte starlette<0.51.0 -> ResolutionImpossible.
fastapi==0.136.3
uvicorn[standard]==0.24.0
starlette==1.3.1
# Pflicht, nicht optional: classification.py nimmt einen UploadFile entgegen, ohne
# python-multipart bricht der Import der App beim Start ab.
# Wie im Root-Manifest auf/ueber 0.0.31 halten (Dependabot-Hinweis #288).
python-multipart==0.0.31

# Pydantic for data validation
pydantic==2.11.7
pydantic-settings==2.10.1
email-validator==2.2.0

# Database (read-only access to main DB)
SQLAlchemy==2.0.41
psycopg2-binary==2.9.10
asyncpg==0.30.0

# HTTP client for backend communication
httpx==0.28.1
aiohttp==3.14.3

# AI/ML Core
langchain-core>=0.3.65,<2.0.0
langgraph==1.2.4
langgraph-checkpoint-sqlite==3.1.1
openai==1.91.0

# Vector Store & Embeddings
# chromadb 0.5.23 hat GHSA-xph7-9rjv-w5fr; Fixversion existiert nicht (last
# affected 1.5.9, Patch-PR noch offen). Pin bleibt, Befund bleibt sichtbar.
chromadb==0.5.23
sentence-transformers==3.3.1
# Explizit 4.46.3: chromadb 0.5.23 verlangt tokenizers<=0.20.3, transformers
# ab 4.47 zieht tokenizers>=0.22. Hoeher pinnen wuerde das Manifest unaufloesbar
# machen. Die Advisories von 4.46.3 bleiben sichtbar, solange chromadb 0.5.x
# gehalten wird.
transformers==4.46.3
tiktoken==0.8.0

# Logging and monitoring
python-json-logger==2.0.7
structlog==24.4.0
prometheus-client==0.19.0

# Security
pyjwt==2.13.0

# Configuration
python-dotenv>=1.2.2,<2.0.0

# Redis for caching
redis==6.2.0

# Utilities
click==8.3.3
PyYAML==6.0.1


