# AI CRM Service

Responsibilities:
- Wrap shared AI platform with CRM-specific prompts, guardrails and telemetry.
- Expose HTTP APIs used by CRM front-end/back-end (`compose-email`, `summarize-thread`, `next-best-actions`, `rag/search`).
- Persist prompt templates + audit logs; interact with RAG index.
