# Workflow Mock Service

Minimaler FastAPI-Service für Integrationstests (nicht für Produktion bestimmt).

- `POST /api/v1/workflows/definitions` → akzeptiert Definitionen und antwortet mit `status=registered`.
