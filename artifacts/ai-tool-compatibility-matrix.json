{
  "generated_at": "2026-06-23",
  "purpose": "AI tool and model compatibility matrix for VALEO agentic development",
  "policy": {
    "critical_context_must_be_repo_persisted": true,
    "chat_only_decisions_forbidden": true,
    "sensitive_data_requires_local_or_approved_provider": true,
    "external_model_output_requires_tests_and_review": true
  },
  "tools": [
    {
      "name": "Codex",
      "role": "coding-agent",
      "allowed_use": ["repo edits", "tests", "documentation", "governance scripts"],
      "restrictions": ["no unreviewed production secrets", "no irreversible external actions without explicit approval"],
      "fallback": "Claude Code or manual implementation using persisted slice contracts"
    },
    {
      "name": "Claude Code",
      "role": "parallel coding-agent",
      "allowed_use": ["domain implementation", "frontend/backend slices", "reviews"],
      "restrictions": ["must respect Workboard file ownership", "must not bundle foreign WIP"],
      "fallback": "Codex using Workboard handoff and slice YAML"
    },
    {
      "name": "Aider",
      "role": "multi-file refactoring",
      "allowed_use": ["planned refactorings", "mechanical code changes"],
      "restrictions": ["requires scoped file list", "no broad unreviewed rewrites"],
      "fallback": "Codex patch-based edits"
    },
    {
      "name": "Continue.dev",
      "role": "codebase navigation",
      "allowed_use": ["repo understanding", "developer onboarding", "search"],
      "restrictions": ["not source of truth", "must verify with files/tests"],
      "fallback": "ripgrep, docs, IDE navigation"
    },
    {
      "name": "Local models",
      "role": "privacy-preserving assistance",
      "allowed_use": ["sensitive summaries", "repeatable local workflows", "cost-sensitive tasks"],
      "restrictions": ["must pass same tests and reviews", "no lower quality gate"],
      "fallback": "approved external model with redacted context"
    }
  ],
  "data_classes": [
    {
      "class": "public_repo_context",
      "external_model_allowed": true,
      "requirements": ["no secrets", "no customer PII"]
    },
    {
      "class": "business_confidential",
      "external_model_allowed": "approved_provider_only",
      "requirements": ["minimize context", "persist decisions in repo", "review output"]
    },
    {
      "class": "personal_data_or_payroll",
      "external_model_allowed": false,
      "requirements": ["local processing or anonymized synthetic data", "DSGVO review"]
    },
    {
      "class": "secrets_credentials_tokens",
      "external_model_allowed": false,
      "requirements": ["never paste", "rotate if exposed"]
    }
  ]
}
