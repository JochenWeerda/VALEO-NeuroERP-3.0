# Mayan-DMS Infrastructure Configuration
# VALEO-NeuroERP Integration
# 
# Kopiere diese Datei zu .env und passe die Werte an:
# cp env.example .env

# --- Pfade (Host-Verzeichnisse) ---
# Diese Verzeichnisse werden auf dem Host erstellt und in Container gemountet
DMS_MEDIA_PATH=./data/mayan/media
DMS_SETTINGS_PATH=./data/mayan/settings
DMS_PG_DATA=./data/mayan/postgres

# --- Netzwerk & Service ---
DMS_HTTP_PORT=8010
DMS_BASE=http://localhost:8010

# --- Bootstrap ---
# Service-Token aus Mayan (im ersten Lauf manuell im UI erzeugen)
# 1. Mayan starten: docker compose up -d
# 2. UI öffnen: http://localhost:8010
# 3. Login: admin / admin (default beim ersten Start)
# 4. Einstellungen → API-Token → "Neues Token erstellen"
# 5. Token hier eintragen:
DMS_BOOTSTRAP_TOKEN=

# --- VALEO-Vorgaben ---
VALEO_BRAND=VALEO NeuroERP

# --- PostgreSQL-Konfiguration ---
POSTGRES_USER=mayan
POSTGRES_PASSWORD=mayan
POSTGRES_DB=mayan

# --- Redis-Konfiguration ---
REDIS_PASSWORD=

# --- OCR-Konfiguration ---
# OCR ist standardmäßig aktiv
# Sprachen: deu (Deutsch), eng (Englisch)
# Weitere Sprachen können im Mayan-UI konfiguriert werden

# --- Mayan-Einstellungen ---
MAYAN_DOCKER_WAIT=30
MAYAN_CELERY_WORKERS=2

