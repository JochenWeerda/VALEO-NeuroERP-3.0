This directory previously pointed to a git submodule (mode 160000).

The submodule metadata was incomplete (missing .gitmodules URL), which breaks actions/checkout cleanup in GitHub Actions.

If this tool is needed again, re-add it as a proper submodule (with .gitmodules) or vendor it as regular source.
