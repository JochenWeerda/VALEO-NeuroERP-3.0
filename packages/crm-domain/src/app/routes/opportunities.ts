import { FastifyInstance } from 'fastify';
import { z } from 'zod';

// Placeholder for opportunity routes
export async function registerOpportunityRoutes(
  fastify: FastifyInstance,
  services: any
) {
  // Opportunity routes will be implemented here
  fastify.log.info('Opportunity routes registered');
}