// Simple test without imports to check if Vitest is working
describe('Analytics Domain', () => {
  it('should be able to run basic test', () => {
    expect(true).toBe(true);
  });

  it('should perform basic arithmetic', () => {
    expect(2 + 2).toBe(4);
  });
});