apiVersion: v1
kind: Service
metadata:
  name: {{ include "valeo-erp.fullname" . }}
  labels:
    {{- include "valeo-erp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "valeo-erp.selectorLabels" . | nindent 4 }}

